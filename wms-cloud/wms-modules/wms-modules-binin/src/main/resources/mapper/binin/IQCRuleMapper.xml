<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bosch.binin.mapper.IQCRuleMapper">


    <select id="getNMDIQCRule" resultType="com.bosch.binin.api.domain.NMDIQCRule">
        select *
        from nmd_iqc_rule nir
        where nir.max_total_quantity <![CDATA[ >= ]]> #{quantity}
          and nir.min_total_quantity <![CDATA[ <= ]]> #{quantity}
          and nir.check_level = #{checkLevel}
    </select>
</mapper>