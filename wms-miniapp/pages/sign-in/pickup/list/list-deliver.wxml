<image class="top-image" src='/img/nutricia-color.png'></image>
<view class="view-class">
  <view wx:if="{{hasData}}" > 
  <text class="title">取货预约已签到列表(近二十条)</text>
  <van-cell-group wx:for="{{list}}" wx:for-item="item" wx:key="pickupId">
    <van-cell center title="{{item.cell}}" value="已签到"
      label="签到时间：{{item.signinDate}}" >
    </van-cell>
  </van-cell-group>
</view>
<view wx:if="{{!hasData}}" > 
  <van-empty description="暂无数据" />
</view>
</view>